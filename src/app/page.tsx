import { CommonBanner } from "@/components/Banner";
import { CommonImageText } from "@/components/ImageText";

const Home: React.FC = () => {
    return (
        <div>
            <CommonBanner />
            <CommonImageText />
        </div>
    );
};

export default Home;
